sudo: required

services:
 - docker

before_install:
 - ls build_scripts/*
 - docker pull dalg24/vera_tpls
 - docker run -v $PWD:/mnt/Futility dalg24/vera_tpls /bin/sh -c "mkdir /build; cd /build; ls /mnt/Futility/build_scripts/; /mnt/Futility/build_scripts/build_on_docker.sh /mnt/Futility; make; ctest"

script:
 - echo $PWD

branches:
  only:
    - master